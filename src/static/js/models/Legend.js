export class Legend {
    constructor(pickupLocation, pickupDate, returnLocation, returnDate ) {
        this.pickupLocation = pickupLocation;
        this.pickupDate = pickupDate;
        this.returnLocation = returnLocation;
        this.returnDate = returnDate;
    }
}

